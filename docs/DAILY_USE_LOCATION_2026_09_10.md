# GP Work: настройка участка для ежедневной работы

База: `67d6d672a9d384f1f9d2fa03ab99edcc1b30d2da`, опубликованная ветка `codex/gp-work-security-browser-v3`.
Рабочая ветка: `codex/gp-work-daily-use-v4`.

Во время работы в репозитории появился независимый релиз main `fd75de9e6179712390440f43fe4dc1007a045072`; `/version.json` сайта подтвердил эту новую web-версию. Данный merge/deploy выполнен вне этой задачи. Новая main-база объединена только **в рабочую ветку** с сохранением обеих историй: настройки двух форм, проценты табеля, исправления Excel и защищённого nginx не теряются. Сам main и production этой задачей не изменяются. Требуется CI итогового объединённого commit; первоначальное наблюдение production ниже сохранено с датой.

## Подтверждённое исходное состояние

- 10 сентября 2026 production `/api/health` и `/version.json` ответили HTTP 200 с одной версией `59cfe2aa18606533beb1f970dcf92576b1fc0599`; база данных доступна.
- CI базы `67d6d67` завершился успешно: [run 34391847034](https://github.com/talgat-maratuly/gp-work-/actions/runs/34391847034). В логах: PostgreSQL 18.6, 25 миграций на чистой базе, 32 unit, 3 серверных E2E, 2 браузерных сценария.
- [Проверка сервера 9 сентября](https://github.com/talgat-maratuly/gp-work-/actions/runs/34391847173) показала 1 активный участок, у которого отсутствовали координаты, и `NGINX_UPLOADS_PRIVACY_READY=False`. Это историческое наблюдение, не повторная проверка БД 10 сентября.
- В браузере открыта страница входа. Вход отменён пользователем; внутренние production-страницы и реальные данные за текущую дату не проверены.

## Устранённый разрыв

Новая проверка смены требует координаты участка, но в интерфейсе их нельзя было задать. API принимал их только при PATCH, а `mapSection` отбрасывал координаты из ответа. Пустые строки и `null` также могли обходить корректную проверку местоположения.

- Создание и редактирование участка содержат широту, долготу и радиус. Координаты можно ввести вручную, включая десятичную запятую, либо получить с устройства, находясь на участке.
- Неточная GPS-точка и отказ разрешения не заменяют введённые значения. Определение координат не сохраняет участок автоматически.
- API принимает координаты при создании и обновлении, проверяет диапазоны, отклоняет пустые/null/логические значения. Координаты изменяются только парой. Радиус: 10–5000 м; прежний радиус 150 м по умолчанию сохранён.
- Существующие координаты отображаются после перезагрузки. Изменение местоположения сохраняет прежний QR. Код участка и исторические данные не меняются.
- Участок можно сохранить без координат для последующей настройки. В списке и на экране QR работник видит причину недоступности смены. После настройки можно обновить данные без повторного входа.
- Миграция БД для этого изменения не требуется: используются существующие поля.
- В мобильное меню добавлены профиль, выход и закрытие меню. Декоративная строка поиска без обработчика убрана. Браузерный тест проверяет выход и запрет повторного открытия защищённой страницы без входа.

## Проверки

Локально пройдены API/web production build, 56 unit-тестов, проверки ролей/действий, 4 проверки преобразования координат и typecheck серверных E2E. Для нового поведения подготовлены PostgreSQL E2E и браузерные тесты desktop/mobile: реальная форма, отказ и неточность GPS, повторное чтение, редактирование и сохранность QR. Результат нового CI фиксируется после его выполнения.

Первый CI [34513179596](https://github.com/talgat-maratuly/gp-work-/actions/runs/34513179596) подтвердил 56 unit и 4 PostgreSQL E2E, но обнаружил перекрытие кнопки сохранения полями координат на мобильном экране. Форма редактирования исправлена: местоположение и действия вынесены из общей сетки, для узкого экрана явно задан один столбец. Браузерный тест сохраняет обычное нажатие; принудительное нажатие и отключение проверок не используются. Повторный CI [34514075569](https://github.com/talgat-maratuly/gp-work-/actions/runs/34514075569) на `7f85b5b` прошёл полностью, включая 4 браузерных сценария. Дополнение мобильного выхода проверяется следующим CI на точном commit ветки.

Проверка мобильного выхода также прошла: [CI 34514543391](https://github.com/talgat-maratuly/gp-work-/actions/runs/34514543391) на `a77ccb1` завершён успешно. После включения независимого релиза `fd75de9` локально подтверждены сборки, 58 unit, компиляция E2E и 2 проверки конфигурации nginx; итоговое объединение проходит отдельный CI.

Браузерные тесты используют отдельную тестовую БД и учётные записи; GPS и камера моделируются, ответы бизнес-API не подменяются. Это не проверка настоящего iPhone или production-данных.

## Страницы действующего приложения

| Страница | Назначение |
|---|---|
| `/admin` | Сводка руководителя |
| `/admin/objects` | Объекты, участки, QR и настройка местоположения после установки новой версии |
| `/admin/tasks` | Назначение работ |
| `/admin/routes` | Маршруты бригад |
| `/field/today` | День работника |
| `/admin/work-days` | Приёмка рабочих дней |
| `/admin/attendance` | Табель |

Адрес: https://gp-work.gpartners.kz. Каждая страница требует соответствующей роли и входа.

## Что осталось до использования на сервере

1. Указать настоящие координаты и радиус действующего участка. Они не подставляются из тестовых данных.
2. После отдельного разрешения владельца установить API/web одной проверенной версии с резервной копией и обновлением nginx для защищённых фотографий. Main и production данным изменением не меняются.
3. Пройти на настоящем iPhone QR → GPS → камера → начало → результат → приёмка на реальном участке. Автоматическое распознавание личности здесь не реализовано: три кадра проверяет руководитель.

GP Service, GP Platform и GP AI Operating Platform не изменены.
