# GP Work QA defect journal

Этот журнал фиксирует первопричину каждого найденного класса дефектов, системное исправление и защиту от возврата. Статус `закрыт локально` не означает production-ready: PostgreSQL E2E, CI, test deploy и повторные циклы учитываются отдельно.

## 2026-09-05 — текущий цикл `codex/worker-day-cycle-v2`

### QA-001 — дата бизнеса зависела от UTC/часового пояса сервера

- Симптом: около полуночи в Уральске dashboard, маршруты и отчёты могли показывать предыдущий день; frontend использовал UTC через `toISOString()`.
- Первопричина: несколько независимых реализаций «сегодня» на backend и frontend.
- Исправление: единая бизнес-дата `Asia/Oral`, строгая проверка календарной даты, единый расчёт дня/недели/месяца во всех затронутых модулях.
- Regression: unit-тесты границы Oral/UTC, перехода недели через месяц, високосного февраля и несуществующей даты.
- Статус: закрыт локально.

### QA-002 — водовоз попадал на недоступный dashboard

- Симптом: после входа роль `WATER_CARRIER` перенаправлялась на `/admin`, где API возвращал `403`.
- Первопричина: домашний маршрут не учитывал специализированную роль; пункт dashboard был виден без ограничения.
- Исправление: домашний маршрут водовоза — `/admin/watering`; недоступный dashboard скрыт и защищён.
- Regression: автоматический тест матрицы маршрутов проверяет доступ водовоза только к поливу и запрет dashboard/tasks.
- Статус: закрыт локально.

### QA-003 — доказательством считалась произвольная строка URL

- Симптом: Face, фото ДО/ПОСЛЕ и фото смены можно было записать ссылкой на внешний или отсутствующий файл.
- Первопричина: бизнес-методы доверяли URL из DTO и не связывали его с UploadsService.
- Исправление: все новые evidence-ссылки проверяются как файл GP Work: допустимый путь/origin, безопасное имя, существование, ненулевой размер и совпадение magic bytes с расширением.
- Regression: тесты реального сохранённого JPEG, отсутствующего файла, внешнего origin и path traversal.
- Статус: закрыт локально.

### QA-004 — Face/liveness допускал один кадр и не имел пути пересъёмки

- Симптом: API принимал один кадр; отклонённый Face всё равно позволял начать работу; мобильный экран скрывал пересъёмку.
- Первопричина: DTO и сервис проверяли только наличие evidence, не статус и не три уникальных кадра.
- Исправление: ровно три разных кадра; отклонённый последний Face блокирует start/complete; причина отклонения обязательна; worker UI позволяет пройти три кадра заново.
- Regression: PostgreSQL E2E отклоняет Face без причины, блокирует старт после отклонения и продолжает только после новой тройки кадров.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-005 — результаты смены не валидировались и не сохранялись как данные

- Симптом: вложенные проценты обходили validation; неизвестные/повторные задачи и результаты других участков искажали общий процент; детали существовали только внутри audit event.
- Первопричина: отсутствовали `ValidateNested`, проверка области задач и отдельная колонка результатов.
- Исправление: отдельный DTO закрытия, nested validation, проверка дублей/назначения/полноты, обязательные описание и причина частичного выполнения, JSONB `task_scope` и `task_results`, расчёт процента только из проверенного набора.
- Regression: PostgreSQL E2E проверяет forged URL, процент `101`, дубли, сохранение результата и идемпотентное повторение.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-006 — возвращённая смена была тупиковым состоянием

- Симптом: после `RETURNED` работник не видел смену и не мог повторно отправить исправление.
- Первопричина: state искал только `OPEN`, а close разрешал только первое закрытие.
- Исправление: `RETURNED` снова отображается с причиной; разрешена доказательная повторная отправка; статус возвращается в `CLOSED`; приёмка разрешена только завершённой смене; возврат без причины запрещён.
- Regression: E2E проходит `CLOSED → RETURNED → RESUBMITTED → REVIEWED` и проверяет запрет возврата без причины.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-007 — физический QR и детальная задача были двумя несвязанными потоками

- Симптом: `/field/scan/:sectionCode` открывал смену, но не переводил к реальному task execution с QR/GPS/Face/фото/чек-листом.
- Первопричина: экран смены показывал задачи текстом без действий.
- Исправление: каждая задача в открытой смене ведёт в существующее execution либо в QR/GPS-подтверждение с уже проверяемым кодом физического участка.
- Regression: web production build и PostgreSQL E2E полного field cycle; интерактивный browser smoke остаётся обязательным.
- Статус: закрыт по коду, browser smoke не доказан.

### QA-008 — детальное выполнение автоматически записывало `100%`

- Симптом: work log сохранял `100%`, хотя worker не передавал результат; после доработки журнал оставался со старыми данными.
- Первопричина: completion DTO не содержал результата, а запись была захардкожена.
- Исправление: completion принимает только явно подтверждённые 100%, обязательное описание и фактический объём; execution хранит эти поля; повторная отправка обновляет ту же work-log запись и сбрасывает её на повторную проверку; после возврата требуется новое фото ПОСЛЕ.
- Regression: E2E запрещает старый пустой payload, проверяет возврат с причиной, запрет повторной отправки без нового фото, обновление work log без дубля и итоговый `APPROVED`.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-009 — скрытый пункт меню не защищал URL

- Симптом: роль могла вручную открыть скрытую admin-страницу и получить экран с `403`/failed requests.
- Первопричина: role filtering существовал только в `AdminLayout`, а большинство nested routes наследовали слишком широкий guard.
- Исправление: единая `ADMIN_ROUTE_ROLES` применяется и к меню, и к каждому маршруту.
- Regression: dependency-free Node test проверяет водовоза, Акимат/Антикор и наследование ADMIN директором.
- Статус: закрыт локально.

### QA-010 — отказ карты скрывал весь dashboard

- Симптом: необязательный запрос диспетчерской карты в `Promise.all` переводил всю страницу в error state, даже когда основная сводка была доступна.
- Первопричина: обязательная и дополнительная части экрана имели общую точку отказа.
- Исправление: dashboard сохраняет основную бизнес-сводку и показывает отдельное предупреждение о карте.
- Regression: TypeScript production build; компонентный browser test ещё требуется.
- Статус: закрыт по коду, browser smoke не доказан.

### QA-011 — поддельная будущая дата обходила новое фото после возврата

- Симптом: заранее загруженное фото ПОСЛЕ с будущим `capturedAt` могло считаться доказательством исправления после возврата.
- Первопричина: свежесть доказательства сравнивалась по времени, присланному мобильным клиентом.
- Исправление: повторное завершение использует неизменяемый серверный `createdAt` загрузки фото.
- Regression: PostgreSQL E2E заранее загружает фото с будущей датой и доказывает, что завершение всё равно запрещено до новой серверной загрузки.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-012 — пустые HTML-действия не имели системной защиты

- Симптом: ручной поиск мог пропустить декоративную кнопку, пустой `onClick` или ссылку `href="#"`.
- Первопричина: production build проверяет типы, но не доказывает наличие действия у HTML-контрола.
- Исправление: AST-аудит всех TSX-файлов проверяет кнопки, submit-контекст и реальные цели ссылок.
- Regression: `npm run test:web:actions` выполняется локально и в CI.
- Статус: ожидает первый полный прогон.

### QA-013 — liveness-доказательства смены терялись и могли переиспользоваться

- Симптом: смена сохраняла только число кадров, а отклонённую Face verification можно было повторить теми же файлами под новым operation id.
- Первопричина: URL стартового/финального liveness не входили в модель смены, а свежесть кадров не проверялась системно.
- Исправление: три кадра вместе с основным селфи сохраняются в JSONB и audit event; кадры нового этапа не могут пересекаться с ранее использованными; после возврата нужны новые liveness-кадры и хотя бы одно новое фото результата; приёмка показывает все доказательства.
- Regression: unit-тесты общего liveness-правила и PostgreSQL E2E на повторное использование, сохранение и доказательную повторную отправку.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-014 — password hash мог попасть во вложенные API-ответы

- Симптом: `/users` вручную маскировал hash, но другие запросы возвращают связанные `user`/`worker` сущности напрямую.
- Первопричина: `passwordHash` был обычной selectable TypeORM-колонкой; безопасность зависела от ручного mapping каждого endpoint.
- Исправление: колонка глобально `select: false`, а login явно запрашивает hash только для bcrypt; минимальный новый пароль увеличен с 4 до 8 символов в API и UI.
- Regression: полный E2E продолжает логиниться, отклоняет пароль из 4 символов и проверяет отсутствие `passwordHash` в user/execution JSON.
- Статус: ожидает PostgreSQL E2E в CI.

### QA-015 — E2E нарушал реальный batch-контракт загрузки и упирался в throttling

- Симптом: PostgreSQL E2E падал с `429` во второй цепочке после успешных build, migrations и первого business flow.
- Первопричина: тест загружал каждый liveness-кадр отдельным запросом, тогда как API и UI поддерживают до 10 файлов в одном запросе; общий IP теста превышал лимит 20 запросов/мин.
- Исправление: E2E загружает связанные кадры пакетами так же, как production web; разные аутентифицированные роли моделируются отдельными proxy IP, сохраняя production rate limit для каждого клиента.
- Regression: обе доказательные цепочки должны пройти подряд с production throttling guard.
- Статус: ожидает повторный PostgreSQL CI.

### QA-016 — удаление сотрудника разрушало или блокировало связанную историю

- Симптом: UI предлагал hard delete, хотя смены, executions и Face evidence имеют `RESTRICT`, а часть старых связей — `SET NULL`/`CASCADE`; результатом могли быть `500` или потеря связности аудита.
- Первопричина: lifecycle учётной записи смешивал блокировку и физическое удаление.
- Исправление: DELETE стал восстановимой деактивацией; UI оставляет единое блокировать/разблокировать; запрещены самоблокировка, самопонижение и отключение последнего ADMIN/DIRECTOR.
- Regression: PostgreSQL E2E проверяет, что DELETE сохраняет строку, запрещает login и не позволяет отключить текущего администратора.
- Статус: ожидает E2E.

### QA-017 — контрольные роли появлялись как исполнители задач

- Симптом: `AKIMAT`, `ANTICOR` и `DIRECTOR` могли попасть в dropdown назначения задач.
- Первопричина: assignees исключал только `ADMIN` вместо allowlist полевых ролей.
- Исправление: endpoint возвращает только активных WORKER, WATER_CARRIER, BRIGADIER и AGRONOMIST.
- Regression: PostgreSQL E2E проверяет исключение контрольной роли.
- Статус: ожидает E2E.

### QA-018 — пробельные ФИО/логин проходили DTO и становились пустыми после trim

- Симптом: `"   "` удовлетворял `IsNotEmpty`, затем service сохранял пустое значение после `.trim()`.
- Первопричина: нормализация выполнялась после validation.
- Исправление: ФИО и логин trim-ятся через `Transform` до проверки; новые пароли минимум 8 символов и содержат хотя бы один непробельный символ.
- Regression: PostgreSQL E2E отклоняет создание пользователя с пробельным ФИО.
- Статус: ожидает E2E.

### QA-019 — состав бригады расходился между двумя источниками

- Симптом: `brigade_members` и `users.brigade_id` показывали разный состав после редактирования пользователя или повторного сохранения бригады; permissions использовали устаревший `brigadeId`.
- Первопричина: синхронизация добавляла новые связи, но не очищала старые, и выполнялась неатомарно.
- Исправление: обе стороны обновляются в одной транзакции; ответ бригады строится из канонического `users.brigade_id`; проверяются существование, активность и полевая роль; бригадир автоматически входит в состав и обязан иметь роль BRIGADIER.
- Regression: PostgreSQL E2E проверяет состав и запрет контрольной роли в качестве бригадира.
- Статус: ожидает E2E.

### QA-020 — удаление бригады могло оборвать действующие связи

- Симптом: UI вызывал физический DELETE без error handling, хотя задачи, маршруты и сотрудники ссылаются на бригаду.
- Первопричина: lifecycle смешивал удаление справочника и прекращение активности.
- Исправление: DELETE/кнопка выполняют деактивацию, история и связи остаются; UI показывает статус, loading/error/empty/success и позволяет восстановить бригаду.
- Regression: PostgreSQL E2E проверяет `isActive=false` после DELETE при сохранении задачи и состава.
- Статус: ожидает E2E.

### QA-021 — legacy WorkForm позволял анонимно создавать недоказанные work logs

- Симптом: публичный `POST /work-logs` принимал произвольные ФИО, участок, процент, GPS-флаг и URL фото без login, назначения, geofence, Face, checklist или stored-file validation.
- Первопричина: старая QR-форма осталась параллельным write-flow после появления evidence execution.
- Исправление: анонимный/manual write endpoint и неиспользуемый `tasks/open` удалены; legacy URL после авторизации перенаправляется в единственный `/field/scan/:sectionCode` flow.
- Regression: PostgreSQL E2E доказывает `404` для удалённого `POST /work-logs` и `401` для анонимного чтения участка; production build доказывает защищённый legacy redirect.
- Статус: ожидает E2E и browser smoke.

### QA-022 — удаление объекта/участка разрушало историю и оставляло действующий QR

- Симптом: DELETE объекта физически удалял его участки каскадом, а DELETE участка мог удалить запись вместе со связанными задачами; QR и полевой сценарий не имели явного жизненного цикла.
- Первопричина: прекращение эксплуатации справочника было реализовано как hard delete, а признаки активности отсутствовали в модели, API и UI.
- Исправление: добавлены `is_active` и аддитивная миграция; DELETE теперь архивирует с сохранением истории, деактивация объекта транзакционно архивирует его участки, повторная активация выполняется явно; QR/начало полевого действия для архива запрещены, UI показывает статус и действия «В архив»/«Активировать».
- Regression: PostgreSQL E2E проверяет валидацию геозоны, сохранение задачи после архивации объекта и участка, блокировку QR/field scan, запрет участка в архивном объекте и явное восстановление.
- Статус: ожидает build и PostgreSQL E2E в CI.

### QA-023 — CRUD задач обходил назначение, permissions и state machine

- Симптом: клиент мог создать задачу сразу в `VERIFIED`, generic PATCH менял статус/исполнителя уже принятой задачи, контрольные роли и архивные справочники принимались как исполнители/основания, а DELETE физически удалял задачу либо падал на `RESTRICT`.
- Первопричина: create/update/remove были общим CRUD без единого валидатора назначения и без actor-aware проверки владельца бизнес-процесса.
- Исправление: единая валидация требует активные участок/объект/вид работы/исполнителя/бригаду и согласованное членство; статус создаётся только `ASSIGNED` и меняется только отдельными workflow endpoints; редактирование возможно до принятия и в разрешённом scope; DELETE стал отменой `CANCELLED`, сохраняет историю и транзакционно пропускает соответствующую точку маршрута; бригадир видит и назначает только свою бригаду, DIRECTOR наследует ADMIN также на service-уровне.
- Regression: PostgreSQL E2E отклоняет поддельный статус, контрольную роль, архивный вид работы, изменение принятой задачи и архивирование с активной задачей; доказывает `CANCELLED`, сохранение task row, `SKIPPED` route stop и отсутствие отменённой задачи в field today.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-024 — одна задача могла находиться в нескольких активных маршрутах

- Симптом: создание маршрута не проверяло существующие незакрытые route stops, актуальность участка/вида работы/исполнителя и позволяло бригадиру указать чужую бригаду.
- Первопричина: маршрут проверял только наличие task id и часть статусов, не проверяя операционную целостность всей цепочки.
- Исправление: route create загружает и проверяет связанные сущности, допускает только `ASSIGNED/ACCEPTED`, запрещает повтор в активном маршруте и ограничивает бригадира своей бригадой; UI показывает только допустимые задачи/бригады и явный empty state.
- Regression: PostgreSQL E2E запрещает повторное включение task, включение отменённой task и создание маршрута бригадиром для чужой brigade id.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-025 — блокировка редактируемой задачи падала на nullable relations

- Симптом: PostgreSQL E2E получал `500` вместо бизнес-ответа при PATCH задачи; PostgreSQL отклонял `FOR UPDATE` для nullable-стороны `LEFT JOIN`.
- Первопричина: pessimistic lock применялся к TypeORM-запросу, который одновременно загружал все nullable relations задачи через outer joins.
- Исправление: транзакция блокирует только основную строку `tasks`; необходимые идентификаторы уже находятся в ней, а связанные сущности отдельно проверяет единый assignment validator.
- Regression: полный PostgreSQL field-cycle вызывает запрещённый PATCH принятой задачи и требует контролируемый `400`, не `500`.
- Статус: ожидает повторный PostgreSQL E2E в CI.

### QA-026 — удаление вида работы обнуляло связь исторических задач

- Симптом: красная кнопка «Удалить» физически удаляла `work_types`; `tasks.work_type_id` переходил в `NULL`, а отчёты и доказательства теряли бизнес-классификацию.
- Первопричина: прекращение использования справочника было реализовано как hard delete и дублировалось отдельной кнопкой рядом с выключением.
- Исправление: DELETE сохраняет строку и переводит её в архив; архивирование запрещено, пока есть незавершённые задачи; создание задачи и архивирование синхронизированы блокировкой строки справочника; UI оставляет одно понятное действие «В архив»/«Активировать» и показывает backend-ошибку.
- Regression: PostgreSQL E2E доказывает сохранение архивированной строки, запрет новых задач с ней и запрет архивирования вида с активными задачами; функциональный unique index защищает нормализованное имя от case/space-дубликатов и гонок.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-027 — публичный checkout позволял закрыть табель другого сотрудника

- Симптом: анонимный endpoint показывал ФИО и время прихода всех активных работников, а POST принимал чужой attendance id или свободно введённое имя и отмечал уход без login, QR участка, geofence, selfie и итогов смены.
- Первопричина: legacy табель существовал как параллельный публичный бизнес-процесс рядом с доказательным `work-days` flow и связывал записи по изменяемому ФИО.
- Исправление: публичные active-today/check-out/общий QR удалены; табель связан с `users.id` и транзакционно синхронизируется только при доказанном открытии/закрытии рабочего дня; старые однозначные записи безопасно backfill-ятся аддитивной миграцией, неоднозначные остаются историческими.
- Regression: PostgreSQL E2E требует `404` от удалённых legacy endpoints, `401` от табеля без login и доказывает переход `ON_DUTY → COMPLETED` с user id и GPS после реального start/close work-day.
- Статус: ожидает build и PostgreSQL E2E в CI.

### QA-028 — бригадир видел и принимал чужие рабочие дни

- Симптом: endpoint списка возвращал все work-day sessions, а review проверял только название роли; бригадир без бригады также получал глобальный доступ.
- Первопричина: controller не передавал actor в service, поэтому row-level permission scope отсутствовал.
- Исправление: список и приёмка стали actor-aware; бригадир видит и проверяет только работников своей бригады, а без brigade membership получает пустой список.
- Regression: PostgreSQL E2E логинится бригадиром без бригады, не находит чужую смену и получает `403` при попытке её принять.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-029 — очередь приёмки раскрывала чужие выполнения

- Симптом: review queue возвращала все завершённые выполнения любому бригадиру/агроному; detail отдельно разрешал каждому агроному читать любую работу, хотя review action уже применял более узкий scope.
- Первопричина: read endpoints не использовали единый `canReview` и не получали текущего пользователя.
- Исправление: очередь фильтруется по brigade id для бригадира и creator id для агронома; detail разрешён только исполнителю, допустимому reviewer либо ADMIN/DIRECTOR; бригадир без бригады получает пустую очередь.
- Regression: PostgreSQL E2E проверяет, что посторонний бригадир не видит execution в очереди и получает `403` на detail.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-030 — последовательный E2E ошибочно моделировал всех пользователей одним клиентом

- Симптом: второй PostgreSQL-сценарий получал `429` на login, хотя отдельные пользовательские цепочки были корректны.
- Первопричина: только авторизованные запросы получали отдельные proxy IP; login разных ролей продолжал идти с общего loopback IP и закономерно исчерпывал production rate limit.
- Исправление: тест выдаёт стабильный отдельный proxy IP каждой тестовой учётной записи уже на login, сохраняя реальный лимит для каждого клиента.
- Regression: обе полные E2E-цепочки запускаются последовательно при включённом throttling; ослабление или отключение production-защиты не используется.
- Статус: локальные build/unit/typecheck зелёные; ожидает PostgreSQL E2E в CI.

### QA-031 — dashboard показывал нефильтрованные данные при активных фильтрах

- Симптом: API принимал `objectId`, `brigadeId` и `shift`, но карточки, задачи, полив, график и KPI продолжали считать данные всей организации; некорректные ID превращались в `NaN` вместо контролируемого `400`.
- Первопричина: query-параметры были добавлены только в контракт controller и не проходили DTO-validation и запросы репозиториев.
- Исправление: добавлен строгий DTO даты/периода/ID/смены; фильтры применяются к задачам, просрочке, поливу, графику и management KPI; мастер-карточки считают только активные объекты/участки/бригады; UI позволяет выбрать дату сводки и передаёт её вместе с периодом в связанный экран управления.
- Regression: PostgreSQL E2E отклоняет несуществующую календарную дату и нечисловой ID, доказывает присутствие задач выбранных объекта/бригады и полностью пустой результат для отсутствующего объекта.
- Статус: локальные build/unit/typecheck зелёные; ожидает PostgreSQL E2E в CI.

### QA-032 — dashboard обходил row-level scope задач

- Симптом: бригадир и агроном видели в dashboard описания, исполнителей, KPI и исключения чужих задач, хотя экран задач уже ограничивал их своей бригадой или собственными назначениями.
- Первопричина: dashboard controller не передавал текущего пользователя, а переиспользуемая management-сводка не поддерживала внутренний actor scope.
- Исправление: dashboard стал actor-aware; бригадир автоматически ограничен своей бригадой и не может запросить чужую, бригадир без бригады получает пустой task/schedule scope, агроном видит task KPI/исключения только для созданных им задач.
- Regression: PostgreSQL E2E доказывает пустой dashboard бригадира без бригады и `403` при явном запросе чужой бригады.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-033 — рестарт API откатывал пароль и статус администратора

- Симптом: каждый запуск приложения заново присваивал найденному ADMIN логин и пароль из окружения и принудительно разблокировал его; ручная смена/блокировка отменялась следующим deploy/restart.
- Первопричина: initial bootstrap одновременно использовался как постоянная синхронизация credentials.
- Исправление: bootstrap стал строго одноразовым — при наличии ADMIN он ничего не меняет; явный защищённый reset использует настроенный `ADMIN_USERNAME`; первичный/reset пароль обязан иметь минимум 8 символов.
- Regression: unit-тест доказывает отсутствие `save` и неизменность hash/status существующего ADMIN, а также отказ от слабого первичного production-пароля.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

### QA-034 — auth раскрывал существование заблокированного пользователя и принимал слабый JWT secret

- Симптом: известный заблокированный логин отвечал `403`, неизвестный — `401`; production принимал любой непустой `JWT_SECRET`, включая легко подбираемый.
- Первопричина: состояния учётной записи различались в публичном ответе, а конфигурация проверяла только наличие секрета.
- Исправление: login унифицирован на `401 Неверный логин или пароль`; username нормализуется до DTO-validation; production JWT secret требует минимум 32 байта.
- Regression: PostgreSQL E2E требует `401` после деактивации, unit-тесты отклоняют короткий signing secret и принимают 32-байтовый.
- Статус: ожидает полный локальный цикл и PostgreSQL E2E в CI.

## 2026-09-15 — кайдзен, цикл KZ-001

### KZ-001 — настройка координат теряла контекст открытого участка

- База: main `01a3e34eff7506b754987749e229387444edc3a1`, PR #7 уже включён.
- Подтверждённый факт в коде: ссылка настройки из FieldScanPage вела на общий
  `/admin/objects`. Участок не передавался; требовались повторный поиск и возврат.
- Причина: переиспользовался общий маршрут каталога вместо действия над текущим участком.
- Изменение: встроенный редактор координат и радиуса для ADMIN/DIRECTOR. Общая
  валидация и существующий PATCH /sections/:id; QR и остальные поля не меняются.
  Пустые координаты не считаются успешной настройкой; ошибка сохраняет черновик;
  отмена не отправляет запрос. Права записи смен и координат не расширены.
- Regression: 4 новых сценария desktop/mobile для ADMIN/DIRECTOR в
  tests/browser/section-form.spec.ts: пустой ввод, сетевой отказ и повтор,
  сохранение после reload, отмена изменений, неизменность QR/другого участка,
  вход рабочего и запрет его PATCH. Существующие сценарии наблюдателей проверяют
  отсутствие редактора. Успешные операции используют настоящий API и БД в CI.
- Локально подтверждено: production build API/web; 68 API unit tests;
  проверки web roles/actions/location/director-commands; E2E TypeScript;
  Playwright обнаруживает 34 сценария. `--list` не является выполнением браузерных тестов.
- Ограничения локальной среды: отдельный нативный PostgreSQL не запущен из-за
  запрета смены пользователя; загрузка Chromium завершается сетевыми таймаутами.
  Эти ограничения не являются подтверждёнными дефектами GP Work. Полный PostgreSQL 18
  и браузеры проверяются существующим CI. Его результат привязывается к commit в PR.
- Дополнительная локальная попытка API E2E на одноразовой PGlite: 5/6 сценариев
  прошли; основной цикл остановился на /field/tasks/:id/arrive с 500 после
  `Connection terminated unexpectedly`. Это не проверка на нативной PostgreSQL 18.
  Backend и его E2E-файл в данном commit не менялись. Причина обрыва соединения
  не установлена; полный полевой цикл нельзя считать подтверждённым.
- Публикация новой ветки отклонена автоматической проверкой разрешений: текущего
  поручения, по её решению, недостаточно для отправки исходников в GitHub.
  Повторная отправка и обход через другие инструменты не выполнялись.
- Статус: локальные build/unit/typecheck прошли; браузерные тесты подготовлены,
  но не выполнены; полный API E2E не прошёл; CI заблокирован публикацией.
  Production deploy и живая приёмка в рамках этого цикла не выполнены.
- Закрепление: AGENTS.md задаёт PDCA для дальнейшей работы Codex; docs/KAIZEN.md
  содержит очередь следующих циклов, план пилота и определения метрик.

## 2026-09-15 — KZ-002: единый порядок работы и улучшений

- Поручение: «Давай все внедри вместе». Продолжение KZ-001 в существующем
  GP Work; база main `01a3e34eff7506b754987749e229387444edc3a1`.
- Факт: обычные задачи, полевые доказательства и склад существовали, но не было
  общего порядка со стандартом, подготовкой, владельцем препятствия и проверкой
  улучшения. Потери времени и денежный эффект не измерены.
- Изменение: серверный модуль workflow и раздел «Работа и улучшения»; версии
  стандартов, подготовка/шаги, ответственный/принимающий, доска/WIP,
  препятствия со сроком и независимым подтверждением, PDCA с измерениями и новой
  редакцией инструкции, 5S-регистр инструмента, обзор причин задержек.
  Материалы используют существующие складские движения. Миграция только добавляет
  таблицы и сохраняет историю. Порядок включается явно до прибытия/выполнения.
- Права: проверяются для конкретной задачи. Бригадир ограничен своей бригадой,
  агроном — созданными задачами и полевым назначением, рабочий — назначением.
  Бухгалтер получает только измерения в тенге. Исполнитель не принимает свою работу.
- Найденные при интеграции существенные случаи:
  - Старые start/complete/review позволили бы обходить полевые доказательства;
    для задач с назначенным порядком они отклоняются с указанием QR-маршрута.
  - Проверка лимита и начало работы должны иметь общую транзакцию. Запись
    выполнения, задачи, маршрута и события используют её EntityManager;
    запуск одного работника сериализуется advisory lock.
  - Проверка актуальной версии при принятии предложения выполняется под
    блокировкой вида работы: параллельное улучшение не затирает новую редакцию.
  - Повтор сообщения с тем же UUID и содержимым не создаёт вторую запись;
    подмена содержимого отклоняется. Сетевой отказ сохраняет отправляемый черновик.
  - Неизменённые поля назначения в PATCH не мешают редактированию описания;
    фактическая подмена исполнителя/участка/вида работы после подготовки запрещена.
  - Форма сохраняет ранее назначенную версию стандарта даже после появления новой.

### Проверено локально

- Production build API и web — успешно; API повторно собран после последней правки.
- API unit: 71/71, 12 suites; web roles/actions/location/director-commands: 15/15.
- E2E TypeScript — успешно.
- API E2E: 13/13 в 2 suites на одноразовой PGlite через PostgreSQL wire protocol,
  с настоящим Nest-приложением, миграциями, HTTP-запросами, загрузками тестовых
  файлов и серверными записями. Включены прежние 6 сценариев и новые 7:
  права/область данных; ресурсы/подготовка; препятствие и дедупликация;
  QR → доказательства → пункты → завершение → назначенная приёмка;
  WIP с очередью приёмки; PDCA/версии; одновременные попытки начала работы.
  Тестовые фото проверяют путь загрузки и привязку доказательств, а не точность
  распознавания лица.
- Прежний локальный обрыв PGlite из KZ-001 устранён в тестовом окружении:
  TCP-адаптеру нужен явный maxConnections вместо значения 1 по умолчанию.
  Незавершённая внешняя транзакция также выявила необходимость использовать
  её соединение для записи начала. В финальном прогоне все 13 сценариев проходят.
- Проверка исходника новой миграции на разрушающие операции — успешно;
  миграция действительно применялась при подготовке E2E.
- git diff --check — успешно.
- Playwright обнаруживает 36 сценариев, включая 2 новых desktop/mobile:
  руководитель задаёт порядок, рабочий подтверждает подготовку, сохраняет
  препятствие после сетевого отказа, reload и доска подтверждают серверное состояние.
  Сценарий бухгалтера дополнен просмотром затрат и запретами остальных workflow API.
  Это подготовленные проверки, не выполненные браузерные прогоны.

### Границы результата и выпуск

- PGlite не заменяет PostgreSQL 18: локальный успех не доказывает поведение
  блокировок нескольких независимых backend-процессов на production PostgreSQL.
  Полный тест одновременных стартов оставлен в CI, без отключения или пропуска.
- Chromium локально отсутствует; его загрузка ранее завершалась таймаутами.
  Визуальная/браузерная приёмка новых экранов здесь не выполнена.
- CI дополнен проверкой новой миграции; реальные PostgreSQL 18 и браузерные
  тесты выполняются существующим workflow после разрешённой публикации ветки.
- В рамках KZ-002 push не предпринимался: сохраняется ранее полученный отказ
  автоматической проверки разрешений на публикацию ветки в GitHub без отдельного
  явного разрешения. Обход через другой инструмент не выполнялся.
- CI для нового commit, production deploy и живая приёмка бригадой не выполнены.
  Исходные измерения и эффект компании не подменяются тестовыми данными.
- Инструкция запуска, роли, организация пилота и ограничения:
  docs/WORK_IMPROVEMENT_FLOW.md. Кайдзен применяется к работе Codex через AGENTS.md,
  журнал причин и содержательные проверки; глобальное самообучение не заявляется.

## 2026-09-15 — KZ-003: публикация и настоящий CI

- Владелец ответил «Да» на разрешение отправить
  `codex/kaizen-section-readiness` в GitHub для CI. Прежняя блокировка публикации
  снята этим явным разрешением; main, merge и production в этот этап не входят.
- CLI push не получил учётных данных. Публикация выполнена подключённым GitHub
  Connector. Удалённый commit `ce628caea96b69fef5a6ff9521216edd538daaa6` имеет
  точно тот же tree SHA `113dcab4400cc4f595805bda8847807ed446167e`, что локальный
  проверенный `fd6a494`. Исходные локальные commits сохранены отдельной локальной
  ссылкой; рабочая ветка синхронизирована с опубликованной.
- Первый настоящий CI: https://github.com/talgat-maratuly/gp-work-/actions/runs/35020135124.
  Успешны аудит зависимостей, защита секретов, сборка, unit/web/typecheck,
  миграции и 13/13 API E2E на PostgreSQL 18. Браузер: 34/36 прошли.
- Два отказа: новый workflow-сценарий desktop/mobile ждал
  `getByLabel('Стандарт задачи', {exact:true})` до timeout. Снимки и accessibility
  snapshot показывают видимый combobox с правильным доступным названием; форма
  и данные загружены. Это неверный способ точного поиска select внутри label,
  содержащего варианты, а не доказательство недоступности стандарта пользователю.
- Исправление теста: combobox находится по роли и точному доступному имени;
  сохранены выбор нужной версии, ответственность, проверки сохранения, роли,
  сетевой отказ, дедупликация и доска. Первый select дополнительно ожидается
  через assert visible. Проверки и таймауты не отключены.
- Подтверждённый визуальный недочёт: заголовок «Необходимые материалы» сливался
  с «Добавить материал». Заголовок сделан отдельным блоком.
- Повторный полный CI запускается для исправления. Итоговый GitHub run и его
  точный head SHA являются подтверждением результата; production и реальная
  приёмка бригадой в этих проверках не выполняются.

### KZ-003 — визуальный разбор после зелёного прогона

- Повторный CI https://github.com/talgat-maratuly/gp-work-/actions/runs/35021546456
  для `122ae9d2d94e6ea53c8f4759fb0f92fcc4dd2add` завершился успешно:
  71 unit, 13 API E2E на PostgreSQL 18, 36 browser-сценариев.
- Просмотр сохранённых desktop/mobile снимков выявил отдельный недочёт: доска
  использовала viewport-breakpoints для четырёх колонок внутри узкого FieldLayout.
  На desktop карточка сжималась примерно до 100 px, длинный текст выходил за рамку.
  Успешные действия браузерного теста не проверяли читаемость карточки.
- Исправление: число колонок зависит от доступной ширины контейнера через
  auto-fit/minmax; длинные слова переносятся внутри карточки.
- Regression в существующем desktop/mobile сценарии: ширина карточки не менее
  220 px, содержимое не выходит за её границы, горизонтальная прокрутка страницы
  не появляется. Все прежние проверки сохранены. Следующий полный CI проверяет
  эту дополнительную правку; бизнес-данные и права не меняются.

### BP-001 — пользовательские бизнес-поля и процессы задач

- Запрос владельца: администратор должен создавать поля и бизнес-процессы.
  Факт: существующие form-settings настраивали две отчётные формы; общего
  версионного процесса с переходами, полями и отдельным журналом не было.
- Добавлен конструктор с типизированными полями, этапами, переходами, правами
  просмотра/заполнения и обязательными полями. Его опубликованные версии
  подключаются к реальным задачам через существующий контроль доступа.
- Сохранение и переход выполняются одной транзакцией, со сверкой revision;
  неизвестные поля, недопустимые типы, чужие задачи, переходы без права и
  перезапись устаревшей карточки отклоняются. Значения 0 и false сохраняются.
- Старые экземпляры закреплены за версией; архив не стирает данные; история
  не раскрывает скрытые поля. Завершение бизнес-процесса не заменяет приёмку работы.
- Локально: build API/web; 84 unit; пять role/UI checks; E2E typecheck;
  семь последовательных API E2E на временной PGlite — успешно.
  В тестовых данных исправлено создание задачи без обязательного workTypeId.
- Нативная PostgreSQL локально не запущена из-за EINVAL на chown; Chromium
  не загрузился из-за сетевых тайм-аутов. CI проверяет нативную конкурентную
  запись и полный браузерный цикл на desktop/mobile. Итог CI записывается
  отдельно по точному SHA. Production и реальная приёмка не выполнялись.
- Частный коммерческий план сохранён в прежней локальной ветке
  codex/company-product-roadmap; рабочая ветка конструктора основана на b2fa770.

### BP-002 — точный поиск выпадающих списков в браузерной проверке

- CI 35031250328 на 9703d74: build, 84 unit, 21 API E2E на PostgreSQL 18
  и все 36 прежних браузерных сценариев прошли. Два новых сценария завершились
  тайм-аутом при поиске «Тип поля 1» через getByLabel с exact: true.
- В общем Label текст HTML label содержит вложенные option; это тот же класс
  проблемы поиска select, который зафиксирован в KZ-002. Проверка использует
  роль combobox и точное доступное имя; видимость явно проверяется до выбора.
  Аналогично исправлен поиск шаблона в карточке задачи.
- Проверки обязательных полей, сохранения, отказа сети, повторной отправки,
  ролей и журнала сохранены. Повторный полный CI проверяет продолжение сценария.

### BP-003 — ссылка на задачу терялась при смене пользователя

- На d9fce08 PR CI 35032269914 прошёл (84 unit, 21 API, 38 browser), но push CI
  35032266905 воспроизвёл реальный отказ в обоих новых браузерных сценариях:
  после входа рабочего открывалась /field/today вместо /workflow/tasks/:id.
  Trace и снимок подтверждают правильный аккаунт и существующую назначенную задачу.
- Причина: LoginPage отбрасывала ссылку на задачу при ещё не потреблённом маркере
  logout. Исключение сохраняло только QR участка, поэтому результат зависел
  от момента новой навигации. Успешный параллельный CI не отменяет этот дефект.
- Сохранение ссылки расширено на точный маршрут задачи с положительным ID.
  Кабинет прежнего пользователя по-прежнему отбрасывается; права проверяются
  существующими роутами и API нового пользователя. Права бухгалтера не расширены.
- Регрессия: проверка маршрутов покрывает QR, задачу, старый кабинет и неверные
  URL; browser-сценарий явно оставляет маркер перед новой ссылкой и проверяет
  URL и видимую карточку после входа рабочего и директора. Отрицательная
  проверка отсутствия кнопки больше не может скрыть попадание на другую страницу.
- Следующий полный CI проверяет исправление; production не обновлялся.

### BP-004 — права полей в каталоге шаблонов

- Проверка кода выявила, что каталог для подключения процесса был доступен
  бригадиру/агроному и возвращал описания всех полей, хотя карточка задачи
  уже фильтровала скрытые поля, значения и журнал. Данных экземпляров в каталоге
  не было, но ограничение просмотра метаданных применялось непоследовательно.
- Каталог теперь применяет readRoles и удаляет ссылки на скрытые обязательные
  поля. Администратор/директор сохраняют полный конструктор; сервер подключает
  исходную полную схему, поэтому фильтрация ответа не ослабляет проверки переходов.
- API-регрессия проверяет видимое поле бригадира, отсутствие скрытого поля и его
  идентификатора, полный шаблон директора и запрет публикации бригадиром.

### FD-001 — совместить настройки рабочего дня с конструктором процессов

- Владелец 22.09.2026 (Уральск) разрешил совместить правку Claude с PR #9 и
  выпустить обновление на сайт. Исходная база: 98cdda7, main/production b2fa770.
- Ветка feature/configurable-forms (be83d66) отстала на 37 commits: прямой
  перенос заменял checkout_form, открывал чтение настроек без авторизации,
  отображал все дополнительные типы как текст и не учитывал extra в повторной
  отправке закрытой смены. В production эта ветка не объединялась.
- Функция перенесена точечно: field_day_form — третий независимый ключ;
  work_form и checkout_form сохранены. Рабочий получает настройки через свой
  существующий авторизованный scan API; менять их может администратор/директор.
- Поля текст/число/процент/список/да-нет/комментарий имеют соответствующие
  элементы UI и серверную проверку типов и обязательности. Фото остаётся
  отдельным обязательным доказательством, пользовательский тип photo не предлагается.
  Настройка полей не отменяет QR/GPS, фото лица, результата и права записи смены.
- Дополнительные значения и их подписи сохраняются в существующем JSONB результата
  и видны руководителю при приёмке. Повтор после потери ответа сверяет сохранённые
  значения и не зависит от переименования шаблона; изменение результата отклоняется.
  Исторические подписи остаются после удаления поля из будущей формы.
- Сбой загрузки настроек больше не подставляет локальный кэш/дефолты под кнопку
  сохранения: показаны ошибка и повтор, сохранение заблокировано до успешного чтения.
- Локально: API/web production build, 97 unit, E2E typecheck, 16 проверок
  ролей/маршрутов/действий/геолокации, 2 проверки nginx — успешно.
  Шесть API-сценариев field-cycle на временной PGlite прошли, включая новые
  проверки extra=0/false, неправильных типов, прав, подписей и повторной отправки.
- Новый browser-сценарий проверяет создание полей через UI, отказ загрузки,
  независимость прежних форм, обязательность, настоящий цикл смены, потерю ответа,
  повтор и просмотр сохранённых значений руководителем на desktop/mobile.
  Нативную PostgreSQL 18 и полный браузерный набор подтверждает CI нового commit.
  На этом этапе production ещё не обновлён, приёмка сотрудниками не проводилась.

### FD-002 — повторяющиеся подписи полей и изоляция браузерных клиентов

- Полный CI 35654928969 / 35654923485 на 75c3e86: 97 unit, 21 API E2E,
  сборки и миграции PostgreSQL 18 прошли; browser 36/40.
- Снимок и accessibility snapshot подтверждают, что дополнительные поля
  сохранились после перезагрузки. Реальный дефект страницы: повторяющиеся
  Input/Select/Textarea получали одинаковые id из подписи. Все label ссылались
  на первый элемент, а остальные поля теряли доступное имя. Заданы уникальные
  id по ключу формы/поля; тест проверяет значение после загрузки и уникальность id.
- Другие два отказа: вход рабочего получил HTTP 429 / ThrottlerException.
  Новый сценарий использовал общий имитируемый IP и расходовал лимит соседнего.
  Для него выделен отдельный тестовый клиент, как в существующих сценариях.
  Производственные лимиты не изменены, проверки входа не исключены.
- Просмотрены desktop/mobile снимки формы. Повторный полный CI проверяет
  оставшуюся часть нового сценария (цикл смены, повтор отправки и приёмка).

### JP-001 — свои должности сотрудников (22.09.2026)

- Запрос владельца: самостоятельно добавлять должности в форме сотрудника.
  В базе и UI существовала только фиксированная роль доступа. Исходная версия
  main/production: dcedef41de0ae3989f118d5f09239bc605320d13.
- Добавлен отдельный справочник должностей с созданием, переименованием,
  архивом/восстановлением и nullable назначением сотруднику. Роли, бригады и
  прежние назначения не преобразуются. Архив сохраняет существующие связи.
- Сервер разрешает управление только директору/администратору, нормализует
  названия, предотвращает дубликаты индексом БД, проверяет активность должности
  внутри транзакции назначения. Название должности не участвует в авторизации.
- На странице сотрудников добавлены справочник, отдельные поля «Должность» и
  «Роль доступа», колонка должности и повтор загрузки при сетевой ошибке.
  Вход/профиль также возвращают должность; она видна в блоке текущего аккаунта.
- Локально прошли API/web build, 97 unit, 16 клиентских проверок, E2E typecheck.
  На временной PGlite прежние 21 API-сценарий прошли. Новый набор первоначально
  упёрся в лимит шестого входа; лишний повторный login заменён проверкой /auth/me
  с существующим токеном. Все 5 новых сценариев после этого прошли; лимиты входа
  не менялись. Проверены создание/назначение, валидация/конкурентный дубликат,
  архив/восстановление/очистка, права и отсутствие повышения роли по названию.
- Добавлен браузерный сценарий директора desktop/mobile: создание и назначение,
  перезагрузка, переименование, архив, редактирование сохранённого назначения,
  восстановление/очистка, отказ сети и повтор. Локально Chromium не установился
  (архив загрузки недоступен); браузерную проверку и PostgreSQL 18 выполняет CI.
  На момент этой записи production не обновлён, приёмки сотрудниками нет.
- Инструкция и границы: `docs/JOB_POSITIONS.md`.

### JP-002 — точные подписи полей должности и роли

- CI 35710202498 / 35710249712 на 6bfed3d: сборки, 97 unit, 26 API E2E,
  миграции PostgreSQL 18 и прежние 40 browser-сценариев прошли.
  Два новых сценария остановились на выборе должности: вложенный в label список
  включал варианты в текст подписи; поиск по точной подписи «Должность» не находил
  поле. Запись новой должности до этого шага подтверждена сценарием.
- Подписи должности и роли отделены от select и связаны уникальными htmlFor/id
  для создания и редактирования. Добавлены ранняя проверка доступности полей по
  точным подписям и проверка уникальности id при одновременном открытии двух форм.
  Полный повторный CI проверяет оставшиеся шаги нового пользовательского цикла.

### JP-003 — Мобильная ширина формы сотрудника

- Факт: снимок CI на ширине 390 px показал выход полей формы за границу карточки после добавления длинной должности.
- Причина: неявная колонка CSS Grid учитывала минимальную ширину содержимого.
- Изменение: явная единственная колонка `minmax(0, 1fr)` и сжимаемые контейнеры пароля/списков в обеих формах.
- Проверка: контроль ширины страницы сразу после выбора должности и после архивирования/восстановления входит в сценарий desktop/mobile; итоговый CI фиксируется отдельно.

### WDC-001 — Общая отметка рабочего дня

- Запрос: всем сотрудникам отмечать начало/конец с геолокацией и видеть часы/число людей в табеле.
- Факт: существующий табель автоматически заполнялся полевой формой участка; у офисных ролей не было собственного сценария отметки.
- Изменение: собственные API/экран рабочего дня, GPS с точностью и серверным временем, единая запись с QR-днём, защита от повторных нажатий и чужого аккаунта, сводка табеля по выбранному периоду. Детали: EMPLOYEE_DAY_CLOCK.md.
- Локально: сборки API/web, 97 unit-тестов и проверка типов E2E прошли. В полном прогоне на временном PGlite 29 из 30 API-сценариев прошли; параллельный старт получил 401 в одном запросе. Причина ещё не подтверждена; проверка параллельности на PostgreSQL CI обязательна до выпуска, тест не ослаблен. Два расчётных теста сводки прошли.
- Браузер: добавлены сценарии рабочего/бухгалтера и смены аккаунта во время GPS для desktop/mobile; локальный Chromium недоступен. Итоги CI и выпуска фиксируются отдельно.
- Границы: GPS фиксирует место отметки, часы — интервал между отметками без вычета перерывов; факт выполненной работы остаётся в задачах и приёмке. Реальная приёмка сотрудником на телефоне ещё не проводилась.

### JP-004 — Проверенный выпуск должностей

- CI ветки ca796ed26ff6226551174774bbe442aa73b1baec: оба запуска 35712750055 и 35712755460 успешны; 97 unit, 26 API E2E на PostgreSQL 18 и 42 desktop/mobile browser-сценария.
- PR #10 объединён в main, commit 548997e711a35d0da48de151701088b8c00c7911. Запущены CI и deployment; наличие commit в production проверяется отдельно.

### WDC-002 — Старый незавершённый отчёт не является текущей сменой

- Факт в коде: поиск любой ON_DUTY записи сотрудника мог выбрать историческую автоматическую отметку из журнала. Это мешало начать текущую смену и давало большой интервал при закрытии. Наличие таких записей в production не проверялось.
- Изменение: текущий/переходящий день определяется по явной личной отметке, открытой форме участка либо сегодняшней записи. Исторические отметки только из журнала остаются в истории; они не закрываются и не меняют время автоматически. Прямое закрытие такой старой записи через личный API отклоняется. Миграция добавляет clock_managed=false вместе с полями точности GPS до первого выпуска этой миграции.
- Доказательство: новый API-сценарий с синтетической 30-дневной записью прошёл локально; старые время/статус/часы сохранены, новый день начинается и закрывается отдельно. Сборки и E2E typecheck прошли. Полный PostgreSQL/браузерный CI повторяется для окончательного commit.
- Предыдущая версия ee858591a3c84f65bf6c7c35aaf206932d63531f: CI 35714246034 успешен — 97 unit, 30 API E2E на PostgreSQL 18 и 48 браузерных сценариев desktop/mobile. Параллельный старт на PostgreSQL прошёл; локальный ответ 401 на PGlite не повторился в этом CI.
- Должности: release 548997e711a35d0da48de151701088b8c00c7911 прошёл main CI 35713576581, Deploy API 35713576598 и Deploy Web 35713576536. Production health подтвердил status=ok, database=ok и точный commit; web smoke проверил ту же версию.

### WDC-003 — Однозначная проверка результата и мобильная приёмка

- CI 026898d0ff2a38f5b3e13bc270d573f3b24b8ccc: оба прогона подтвердили 97 unit и 31 API E2E на PostgreSQL; браузерная проверка обнаружила два класса сбоев.
- Собственная отметка: во время обновления экрана одновременно видны сообщения загрузки и успешного сохранения. Тест искал единственный role=status и завершался strict-mode ошибкой. Результат получил имя «Результат отметки»; проверка по этому имени сохраняет проверки реальной записи в API и табеле.
- Приёмка участка: trace зафиксировал многократный перехват клика по кнопке «Подтвердить» соседними элементами и шапкой. Снимок показал выход карточки за ширину 390 px. Смена уже была CLOSED, результат/дополнительные поля сохранены. Причина в минимальной ширине неявной колонки Grid и содержимого карточки.
- Исправление: явная сжимаемая колонка, перенос длинных названий, ограничение карточки и отдельная горизонтальная прокрутка фотографий. Регрессия проверяет ширину документа и настоящий клик подтверждения; force-click и увеличение тайм-аутов не используются.
- Границы: проверяется полный цикл приёмки и отметок на desktop/mobile. Итоги окончательного CI и deployment сохраняются также в PR #11.

### SF-004 — Сохранение формы участка при входе и обновлении приложения

- Сообщение владельца 2026-09-22: «Участки и QR → Открыть» возвращает на главную.
  На приложенном изображении — прежняя форма отчёта. На момент проверки main,
  публичный version.json и загружаемый JS соответствовали 815d4dac; обычный путь
  `/field/scan/:code` уже разрешён директору. Причина именно на устройстве
  владельца не установлена; не считаем сообщение опровергнутым успешным CI.
- Воспроизведено: `resolvePostLoginPath('DIRECTOR', '/field/scan/PIT-003/')`
  возвращал `/admin/director`, ADMIN — `/admin`. Проверка маршрута не допускала
  завершающий слеш, хотя React Router его принимает. Теперь такие ссылки,
  параметры и якорь сохраняются, включая прежний `/work-form/?sectionId=...`.
- Факт в коде: кнопка выхода явно переходила на `/login` без адреса участка;
  возврат зависел от гонки с ProtectedRoute. Общая кнопка выхода теперь передаёт
  разрешённую ссылку участка/задачи явно; кабинет прошлого аккаунта не переносится.
- Факт в коде: любой отказ `/auth/me`, включая сеть/503, удалял токен.
  Теперь временный отказ блокирует защищённое содержимое и предлагает повтор,
  сохраняя адрес. 401/403 по-прежнему требуют нового входа; роль берётся только
  из успешного ответа сервера, а не из локального профиля.
- Подтверждено HTTP-проверкой production: `/sw.js` имел `max-age=2592000` и
  `public, immutable`. Исправлены nginx-правила для sw.js, index.html,
  version.json и manifest; безопасное точечное обновление с backup/nginx -t/
  rollback сохраняет TLS, API, приватную выдачу фото и кэш файлов с хэшем.
  Это дефект доставки обновлений; его причинная связь со скриншотом не доказана.
- Регрессия: вход DIRECTOR/WORKER по ссылке со слешем, сетевой отказ и повтор,
  настоящий выход/вход без повторного открытия QR, истёкший токен, старые QR,
  «Открыть» из объектов. Отдельный CI запускает production build с настоящим
  service worker на desktop/mobile и реальным API/PostgreSQL.
- Локально: web build, две проверки маршрутов и четыре проверки точечной
  настройки nginx прошли. Chromium локально отсутствует; браузерные результаты
  и выпуск фиксируются по точному commit в PR. Приёмка реальным директором и
  рабочим на их устройствах пока не подтверждена.

### PW-001 — Администратор помогает сотруднику восстановить доступ

- Сообщение владельца: в пользователях виден логин, но восстановление сводится к
  полю нового пароля в редактировании. Факт в коде: ручная замена не отзывала
  выданные JWT, не требовала установки личного пароля и не предлагала готовые
  данные для передачи сотруднику.
- Изменение: отдельное действие с подтверждением аккаунта, генерацией и
  копированием временного пароля; срок 24 часа, ограниченный вход и обязательная
  замена. Свою смену пароля может выполнить любая роль с текущим паролем.
  Ссылка QR сохраняется через восстановление и повторный вход. Инструкция и
  границы хранения: PASSWORD_RECOVERY.md.
- Старые пароли/JWT/media-cookie отзываются; временный вход не позволяет читать
  бизнес-данные или фото, даже при роли директора. Обычное редактирование профиля
  не снимает обязательную замену. Добавочная миграция сохраняет текущие учётные
  записи. В БД фиксируется автор и время последнего административного сброса.
- Потеря ответа не выдаётся за успех: администратор явно создаёт другой пароль;
  сотрудник после неопределённого ответа может войти с уже установленным паролем.
  Временный секрет не сохраняется в браузерные хранилища и не отправляется
  автоматически. Закрытие окна удаляет его из интерфейса.
- Локально: сборки API/web, 97 unit-тестов, E2E typecheck и 8 новых API-сценариев
  на временном PGlite прошли, включая права всех ролей, истечение, отзыв токенов,
  ограничения временного входа, конкурентные сбросы и сохранение старого профиля.
  Добавлены desktop/mobile проверки потери ответов, копирования, смены пароля и
  возврата на QR. Их результаты на PostgreSQL 18/Chromium и точный выпуск
  фиксируются в PR после CI. Реальные пользовательские пароли не сбрасывались;
  приёмка сотрудником на его устройстве ещё не подтверждена.

### PW-002 — Доступное имя и размещение окна сброса

- CI commit 418872ab70a0f8018390563c43d5412811da0299 подтвердил 97 unit,
  39 API E2E на PostgreSQL 18 и 54 browser-сценария. Два новых сценария остановились
  после успешного повторного сброса: snapshot содержал пароль и textbox, но dialog
  больше не имел доступного имени. Снимок mobile также показал окно у верхнего
  левого края: сброс стилей убирал стандартные отступы dialog.
- Окну задано постоянное явное aria-label; размещение, отступы от края и максимальная
  высота со скроллом определены явно. Регрессия по доступному имени сохранена,
  добавлены проверки результата и отступов. Ожидание не увеличивалось и реальные
  API-сбросы не подменялись. Проверка всего пути продолжается на новом commit.
- Уточнение по повторному CI f52d7e9b: окно и сообщение об успешном сбросе
  находятся по доступным именам, но точный поиск подписи textarea по-прежнему
  не проходит. Вложенный в label textarea содержит текст значения; он входит
  в текст подписи при таком поиске. Подпись вынесена отдельно и явно связана
  через htmlFor/id. Проверка по точному имени остаётся без ослабления.

### PW-003 — Результат смены пароля не теряется при выходе

- CI 9bac1850: выдача пароля, успешное/запрещённое копирование, хранение только
  в памяти, мобильные отступы, обязательная смена и API-сохранение прошли.
  После сохранения тест не увидел сообщение об успехе на странице входа.
- Причина в порядке обновлений: BrowserRouter установленной версии выполняет
  переход в startTransition, а logout очищал профиль немедленно. ProtectedRoute
  успевал сделать свой переход со ссылкой на /change-password и заменял состояние
  явного перехода (результат и первоначальный QR).
- Очистка профиля и переход после смены пароля объединены в одну transition.
  Полный тест сохраняет проверки сообщения, входа с новым паролем и точного QR
  с завершающим слешем, параметром и якорем. Проверка не ослаблялась.
